homekit:
  auto_start: False
  name: HABridge-Manual
  filter:
    include_entities:
      - alarm_control_panel.home_alarm
  entity_config:
    alarm_control_panel.home_alarm:
      code: !secret alarm_panel_code
